public enum MenuType
{
    Default = 0,
    Inventory = 1,
    Crafting = 2
}

public enum MovementInfluence
{
    Keyboard, MousePosition
}

public enum ActionState
{
    Scurry, Jump, Climb
}
